import React from 'react'
import ReactDOM from 'react-dom/client'
import APIMWebsite from './APIMWebsite.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <APIMWebsite />
  </React.StrictMode>,
)
